/**
 * Reservations Controller (placeholder)
 */

import { Request, Response } from 'express';

export const listReservations = (_req: Request, res: Response): void => {
  res.json({ reservations: [] });
};
