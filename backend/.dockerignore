# Dependencies (rebuilt inside container)
node_modules/
dist/

# Version control
.git/
.github/

# Environment files (injected via docker-compose)
.env
.env.*

# IDE / OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Tests & coverage
coverage/
tests/
jest.config.ts
*.test.ts
*.spec.ts

# Docker files (don't need to be inside image)
Dockerfile
docker-compose*.yml
.dockerignore
